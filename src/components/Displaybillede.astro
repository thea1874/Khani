---
const { baggrund, text } = Astro.props;
---

<div class="container">
  <img src={baggrund} alt="" />
  <h1 class="text-center">{text}</h1>
</div>

<style>
  .container {
    text-align: center;
    position: relative;
    z-index: -1;
  }
  .text-center {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
  }

  h1 {
    color: var(--text-white);
    font-size: var(--font-size-display);
  }
</style>
